{
  "name": "xseed-website-server",
  "version": "1.0.0",
  "description": "Server to host website and send emails",
  "engines": {
    "node": "6.9.x"
  },
  "scripts": {
    "start": "cd react-ui/ && npm install && npm run build && node ../server/index.js",
    "heroku-postbuild": "cd react-ui/ && npm install --only=dev && npm install && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-ui/node_modules"
  ],
  "dependencies": {
    "express": "^4.14.1",
    "nodemailer": "^4.0.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/XseedSF/website"
  },
  "keywords": [
    "node",
    "heroku",
    "create-react-app",
    "react",
    "azure"
  ],
  "license": "MIT",
  "devDependencies": {}
}
